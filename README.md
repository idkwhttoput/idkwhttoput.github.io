# Alex Morgan Portfolio

A responsive personal portfolio built with Next.js, TypeScript, and Tailwind CSS.

## Run locally

```bash
npm install
npm run dev
```

## Deploy to GitHub Pages

Push to the `main` branch, then select **GitHub Actions** as the Pages source in the repository settings. The included workflow builds and deploys the static export automatically.
